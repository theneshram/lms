services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./apps/web:/app
    command: sh -lc "npm ci && npm run dev"
    ports:
      - "3000:3000"
    environment:
      - CI=false