services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: lms-api
    env_file:
      - ./apps/api/.env
    environment:
      PORT: 8080
      CORS_ORIGIN: http://localhost:3000
      MONGO_URI: mongodb+srv://thenesh26_db_user:zS3FVYjeviAvuMoM@cluster0.mjs1u6c.mongodb.net/lms
    ports:
      - "8088:8080"   # host 8088 -> container 8080
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: lms-web
    environment:
      VITE_API_URL: http://localhost:8088
    depends_on:
      - api
    ports:
      - "3000:3000"
